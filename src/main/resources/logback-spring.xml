<configuration>

    <!-- Log folder -->
    <property name="LOG_PATH" value="logs"/>

    <!-- ✅ FIXED: read from Spring -->
    <springProperty name="APP_NAME"
                    source="spring.application.name"
                    defaultValue="unknown-service"/>

    <!-- ================= CONSOLE ================= -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss} %-5level ${APP_NAME} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ================= FILE (ROLLING) ================= -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">

        <file>${LOG_PATH}/${APP_NAME}.log</file>

        <!-- ✅ FIXED rolling policy -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">

            <fileNamePattern>${LOG_PATH}/${APP_NAME}.%d{yyyy-MM-dd}.%i.log</fileNamePattern>

            <maxFileSize>10MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>

        </rollingPolicy>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level ${APP_NAME} - %msg%n</pattern>
        </encoder>

    </appender>

    <!-- ================= ROOT ================= -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>