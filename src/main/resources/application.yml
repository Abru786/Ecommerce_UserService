


server:
  port: 8081

spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://localhost:3306/user_db
    username: root
    password: MYDB4PM
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: INFO
    ms.user_service: DEBUG   # change to your package





eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

